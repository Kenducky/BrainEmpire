server:
    port: 8080

spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/brainempire?useUnicode=true&useSSL=false&characterEncoding=utf8&serverTimezone=Asia/Shanghai
        username: root
        password: 123
    output:
        ansi:
            enabled: ALWAYS
mybatis:
    mapper-locations: com/tonine/mybrainempire/modules/*/dao/mysql/*.xml

logging:
    level:
        com.tonine.mybrainempire: debug